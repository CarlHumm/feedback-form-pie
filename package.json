{
  "name": "feedback-form-pie",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:wordpress": "vite --mode wordpress",
    "build": "vite build",
    "build:wordpress": "vite build --mode wordpress",
    "serve:php": "php -S localhost:8000 -t public"
  },
  "devDependencies": {
    "sass": "^1.89.0",
    "vite": "^6.3.5"
  }
}
